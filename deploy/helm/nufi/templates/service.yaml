apiVersion: v1
kind: Service
metadata:
  name: {{ include "nufi.fullname" . }}
  labels:
    {{- include "nufi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nufi.selectorLabels" . | nindent 4 }}
